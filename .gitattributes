# Git LFS для больших/бинарных файлов
*.psd filter=lfs diff=lfs merge=lfs -text
*.unitypackage filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.anim filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text  # Если текстуры большие (>1-5 MB)

# Unity-файлы (не LFS, но binary, чтобы Git не менял концы строк)
*.meta binary
*.asset binary
*.unity binary
*.prefab binary

# Текстовые файлы (скрипты, шейдеры) — используем LF
*.cs text eol=lf
*.shader text eol=lf
*.json text eol=lf
*.txt text eol=lf
*.md text eol=lf

# Конфиги (если есть)
*.vsconfig text eol=lf

# Изображения (если не в LFS)
*.jpg binary
*.tga binary